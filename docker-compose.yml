services:
  aeroopt-x:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: aeroopt_x_app

    ports:
      - "5000:5000"

    environment:
      PYTHONPATH: /app/src
      FLASK_DEBUG: "0"

    restart: unless-stopped
